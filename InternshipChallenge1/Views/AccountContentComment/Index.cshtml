@model IEnumerable<InternshipChallenge1.Models.AccountContentComment>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Message)
            </th>
            @*<th>
                @Html.DisplayNameFor(model => model.AccountsContentId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AccountsContent)
            </th>*@
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Message)
            </td>
            @*<td>
                @Html.DisplayFor(modelItem => item.AccountsContentId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AccountsContent.AccountsContentId)
            </td>*@
            <td>
                <a asp-action="Details" asp-route-id="@item.AccountContentCommentId">Details</a>
            </td>
        </tr>
}
    </tbody>
</table>
